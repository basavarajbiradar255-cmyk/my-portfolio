# Assets

- Replace `profile-placeholder.svg` with a professional image (for example, `profile.jpg`) and update the image path in `index.html`.
- Add your résumé as `resume.pdf`. The download buttons are already configured for this file.
- Add project images here if you want to replace the styled project visuals.
